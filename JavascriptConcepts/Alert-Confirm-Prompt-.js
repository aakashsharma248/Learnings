const alertData = alert("Check the working of alert message");  // it will only show the okay button and doesn't return anything
console.log(alertData);

const confirmData = confirm("This is a confirm check"); // it will show okay and cancel button and  will return true and false 
console.log(confirmData);

const promptData = prompt("Enter the data");  // it will show okay and cancel button and will return null and input data
console.log(promptData);